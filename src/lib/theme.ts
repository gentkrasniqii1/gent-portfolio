/**
 * Design direction: light-editorial
 * Default theme: light (system preference still respected when user selects "system")
 */
export const THEME = {
  defaultTheme: "light" as const,
  storageKey: "gent-portfolio-theme",
  attribute: "class" as const,
};
